sudo: required
services:
  - docker

before_install:
  - docker build -t rastaehli/client-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true rastaehli/client-test npm test -- --coverage

after_success:
  - docker build -t rstaehli/multi-client ./client
  - docker build -t rstaehli/multi-nginx ./nginx
  - docker build -t rstaehli/multi-server ./server
  - docker build -t rstaehli/multi-worker ./worker
  #push to dockerhub
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin 
  - docker push rstaehli/multi-client
  - docker push rstaehli/multi-nginx
  - docker push rstaehli/multi-server
  - docker push rstaehli/multi-worker
